server {
    listen 3000;
    root /usr/share/nginx/html;
    index index.html;

    location = /callback {
        try_files /callback.html =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
